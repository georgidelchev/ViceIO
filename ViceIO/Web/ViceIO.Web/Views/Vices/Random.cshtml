@model ViceIO.Web.ViewModels.Vices.GetRandomViceViewModel

<h1>Random Vice...</h1>
<a asp-controller="Vices" asp-action="Random"><button class="btn btn-danger">Random</button></a>

    <!--<h5 class="card-title text-center">-->
        @*<a asp-action="ById" asp-route-id="@recipe.Id">
                @recipe.ShortName@if (recipe.Name.Length > 20)
                                 {<text>...</text>}
            </a>*@
    <!--</h5>-->
    <div class="card">
        <div class="card-body">
            <h6 class="card-subtitle mb-2 text-muted">Category: @Model.CategoryName</h6>
            <p class="card-text">@Model.Content</p>
            <p class="card-text">@Model.CreatedOnAsString</p>
            <a href="#" class="card-link">@Model.AddedByUserEmail</a>
        </div>
    </div>
